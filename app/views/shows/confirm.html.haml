= "Really delete #{@show.name}"
= button_to('Yes', url_for(:action => 'destroy', :controller => 'shows',:id => @show.id, :confirm => 'true'), :method => :delete)
= button_to('No', shows_path, :method => :get)