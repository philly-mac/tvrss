- if flash
  #flash
    - if flash[:alert]
      .alert.alert-error= flash[:alert]
    - if flash[:notice]
      .alert.alert-success= flash[:notice]
    - if flash[:warning]
      .alert= flash[:warning]
