- if logged_in?
  .span2
    .sidebar-nav.well
      %ul.nav.nav-list
      %ul
        %li= link_to('Shows', shows_path)
        %li= link_to('Unwatched', shows_path)
        %li= link_to('Cancelled', cancelled_shows_path)
        - if can? :manage, Show
          %li= link_to 'Admin', admin_path
        %li= link_to('Logout', logout_path) if logged_in?
