= error_messages(@user.errors)

%legend Edit Your Registration Details

= form_for(current_user, :html => {:class => 'form-horizontal'}) do |form|
  = render :partial => '/users/form', :locals => {:form => form, :user => @user}
  .controls= button_tag 'Update', :class => "btn btn-primary", :id => 'update-button'
